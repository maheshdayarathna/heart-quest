// Score.js — Mongoose schema for Score
// TODO: Define schema with fields: userId (ref: User), score, level, createdAt

const mongoose = require('mongoose');

// TODO: const scoreSchema = new mongoose.Schema({ ... });
// TODO: module.exports = mongoose.model('Score', scoreSchema);
