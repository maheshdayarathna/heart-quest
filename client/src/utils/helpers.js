// helpers.js — Client-side utility functions
// TODO: Implement reusable helpers (e.g., formatScore, formatDate, validateEmail)

// TODO: export const formatScore = (score) => { ... }
// TODO: export const formatDate = (date) => { ... }
// TODO: export const validateEmail = (email) => { ... }
