// authService.js — Authentication API calls
// TODO: Implement login, register, getProfile functions
// Each function uses the shared api instance (Low Coupling)

// import api from './api';

// TODO: export const login = async (email, password) => { ... }
// TODO: export const register = async (username, email, password) => { ... }
// TODO: export const getProfile = async () => { ... }
