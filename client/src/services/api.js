// api.js — Axios instance with base URL configuration
// TODO: Create and export a configured Axios instance
// TODO: Add request interceptor to attach JWT token from AsyncStorage
// Demonstrates: Low Coupling — centralized HTTP config, all services reuse this instance

// import axios from 'axios';
// import AsyncStorage from '@react-native-async-storage/async-storage';

// const API_BASE_URL = 'http://localhost:5000/api';

// TODO: const api = axios.create({ baseURL: API_BASE_URL });
// TODO: Add interceptors
// TODO: export default api;
