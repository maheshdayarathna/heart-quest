// useAuth.js — Custom hook for accessing authentication context
// TODO: Wrap useContext(AuthContext) for convenient access in components
// Demonstrates: High Cohesion — encapsulates auth logic in one hook

// import { useContext } from 'react';
// import { AuthContext } from '../context/AuthContext';

// TODO: export const useAuth = () => useContext(AuthContext);
