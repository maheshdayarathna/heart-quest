// AuthContext.js — Global authentication state management
// TODO: Create context with user, token, isAuthenticated state
// TODO: Provide login, logout, register functions to children
// Demonstrates: Low Coupling — screens don't manage auth state directly

// import { createContext, useContext, useState } from 'react';

// TODO: export const AuthContext = createContext();
// TODO: export function AuthProvider({ children }) { ... }
