// GameContext.js — Global game state management
// TODO: Create context with gameState, score, level
// TODO: Provide startGame, updateScore, endGame functions
// Demonstrates: Low Coupling — game logic separated from UI components

// import { createContext, useContext, useState } from 'react';

// TODO: export const GameContext = createContext();
// TODO: export function GameProvider({ children }) { ... }
