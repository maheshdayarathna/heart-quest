// ScoreCard.js — Displays a player's score information
// TODO: Display username, score, and rank in a styled card
// Demonstrates: High Cohesion — focused on score display only

// import { View, Text, StyleSheet } from 'react-native';

// TODO: export default function ScoreCard({ username, score, rank }) { ... }
