// Button.js — Reusable button component
// TODO: Create a styled, reusable button with onPress event handler
// Demonstrates: GUI & Event Handling (High Cohesion — single UI responsibility)

// import { TouchableOpacity, Text, StyleSheet } from 'react-native';

// TODO: export default function Button({ title, onPress, style }) { ... }
