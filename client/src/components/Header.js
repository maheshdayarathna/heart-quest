// Header.js — Reusable header/navigation bar component
// TODO: Display app title, navigation controls, and user avatar

// import { View, Text, StyleSheet } from 'react-native';

// TODO: export default function Header({ title, showBack, onBack }) { ... }
